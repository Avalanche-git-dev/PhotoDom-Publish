/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CommentResponseLong } from '../../models/comment-response-long';

export interface CountMainComments$Params {
  photoId: string;
}

export function countMainComments(http: HttpClient, rootUrl: string, params: CountMainComments$Params, context?: HttpContext): Observable<StrictHttpResponse<CommentResponseLong>> {
  const rb = new RequestBuilder(rootUrl, countMainComments.PATH, 'get');
  if (params) {
    rb.query('photoId', params.photoId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CommentResponseLong>;
    })
  );
}

countMainComments.PATH = '/api/comments/count/comments';
